[README]
The Simple Notification Service emails are concatenated into a single file, including
"Launch Notification" and "Termination Notification". The Python script is called
"autoscaling_sns.py".

[RUN]
python autoscaling_sns.py